import 'package:flutter/material.dart';

class ExerciseViewConsts {
  String resultCount = "results founds";
  EdgeInsets screenPadding = const EdgeInsets.symmetric(horizontal: 15);
  double cardHeight = 200.0;
  EdgeInsets cardBottomPadding = const EdgeInsets.only(bottom: 15);
  BorderRadius cardRadius = BorderRadius.circular(10);
  Color generalGreyColor = Colors.grey;
}
